kind: Pipeline
apiVersion: tekton.dev/v1beta1
metadata:
  name: "nbde-tang-server-draft-integration-test"
spec:
  params:
    - description: 'Snapshot of the application'
      name: SNAPSHOT
      default: '{"components": [{"name":"nbde-tang-server", "containerImage": "quay.io/sec-eng-special/tang-operator-bundle:latest"}]}'
      type: string
    - description: 'Namespace where the application is running'
      name: NAMESPACE
      default: "default"
      type: string
    - description: 'Expected output'
      name: EXPECTED_OUTPUT
      default: "Hello World!"
      type: string
  tasks:
    - name: draft-task-1
      description: Placeholder task that prints the Snapshot and outputs standard TEST_OUTPUT
      params:
        - name: SNAPSHOT
          value: $(params.SNAPSHOT)
      taskSpec:
        params:
        - name: SNAPSHOT
        results:
        - name: TEST_OUTPUT
          description: Test output
        steps:
        - image: registry.redhat.io/openshift4/ose-cli:latest
          env:
          - name: SNAPSHOT
            value: $(params.SNAPSHOT)
          script: |
            echo "------------------------------------------"
            dnf upgrade -y
            dnf -y install jq make
            echo "------------------------------------------"
            cat /etc/os-release
            echo "------------------------------------------"
            pwd
            ls
            ls /workspace/
            echo "------------------------------------------"
            oc version
            echo "------------------------------------------"
            printenv
            echo "------------------------------------------"
            set
            echo "------------------------------------------"
            echo -e "Example test task for the Snapshot:\n ${SNAPSHOT}"
            echo "------------------------------------------"
            # Run custom tests for the given Snapshot here
            # After the tests finish, record the overall result in the RESULT variable
            RESULT="SUCCESS"

            # Output the standardized TEST_OUTPUT result in JSON form
            TEST_OUTPUT=$(jq -rc --arg date $(date -u --iso-8601=seconds) --arg RESULT "${RESULT}" --null-input \
              '{result: $RESULT, timestamp: $date, failures: 0, successes: 1, warnings: 0}')
            echo -n "${TEST_OUTPUT}" | tee $(results.TEST_OUTPUT.path)
